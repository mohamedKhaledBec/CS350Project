# Quick Start Guide

## Build
```bash
make all
```

## Run GUI
```bash
make gui
```

## Reset (if needed)
```bash
make reset
```

That's it. The GUI shows CPU, RAM, Disk (%), and Network (packets/sec) with color-coded alerts.
